# Data Wrangling 

---------------

## Subseting Data

### Select Variables (columns)

Use `dplyr::select()`



### Select Observations (rows)

Use `dplyr::filter()`




---------------

## Symbol Opporators

### Logical Opporators

#### Ineqalities (`<`, `>`, `<=`, `<=`)

#### AND ( `&` )

#### OR (`|`)

#### Within a List ( `%in%` )



### The Assignemnt Opporator ( `<-` )


### The Pipe Opporator ( `%>%` )
